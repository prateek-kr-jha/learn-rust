### main: special function, first code to run.
### println! is a rust macro and not function, ! means that you are calling a macro
- rustc: is the compiler
# CARGO:
- cargo build
- cargo run(= cargo build + run)
- cargo check(just check if it compiles but don't create any executable -> faster than cargo build)
- cargo build --release
